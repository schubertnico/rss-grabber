# Die Dateien in inc/ werden nur eingebunden, nie direkt aufgerufen.
# inc/config.php enthält die Zugangsdaten zur Datenbank.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
